services:

    mailer:
        image: node:alpine
        env_file: .env
        ports:
            - 3001:3000
        volumes:
            - ./:/app
        working_dir: /app
        command: npm run dev